{
  "title": "VSSL MS.1 (Experimental)",
  "config": {
    "step": {
      "user": {
        "title": "Add a VSSL MS.1 for field validation",
        "description": "Support is based on synthetic fixtures and must still be confirmed on a real MS.1. Setup remains available if the first probe fails so diagnostics can be downloaded.",
        "data": {
          "host": "IPv4 address",
          "name": "Name",
          "scan_interval": "Refresh interval (seconds)"
        }
      }
    },
    "error": {
      "invalid_host": "Enter a valid unicast IPv4 address"
    },
    "abort": {
      "already_configured": "This address is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "VSSL options",
        "data": {
          "scan_interval": "Refresh interval (seconds)"
        }
      }
    }
  }
}
